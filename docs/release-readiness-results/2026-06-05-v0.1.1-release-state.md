# BugDrop Board v0.1.1 Release State

Status: published and tagged.

BugDrop Board `v0.1.1` is a patch release for the embedded widget UX and install ergonomics polish
that was production-dogfooded on `https://bugdrop.dev`.

## Release Artifacts

- npm package: `@mean-weasel/bugdrop-board@0.1.1`
- npm URL: `https://www.npmjs.com/package/@mean-weasel/bugdrop-board/v/0.1.1`
- npm tarball: `https://registry.npmjs.org/@mean-weasel/bugdrop-board/-/bugdrop-board-0.1.1.tgz`
- GitHub Release: `https://github.com/mean-weasel/bugdrop-board/releases/tag/v0.1.1`
- Git tag: `v0.1.1`
- Release tag target / package publish SHA: `cc3e16f074c8a86aafd37a2e199c811955747467`
- Current post-release `main` includes retry hardening in
  `15d055b ci: extend package release smoke retry window (#30)`.

## Included Changes

- Embedded loading, empty, error, retry, and submit states.
- Accessible upvote labels and `aria-pressed` state.
- Host styling CSS custom-property hooks.
- README embed/install contract updates.
- Production dogfood runbook correction for `wrangler_environment=production`.

## Proof

- Production dogfood receipt:
  `docs/production-dogfood-results/2026-06-05-post-polish.md`.
- Corrected production deploy:
  `https://github.com/mean-weasel/bugdrop-board/actions/runs/27031011338`.
- Package publish workflow:
  `https://github.com/mean-weasel/bugdrop-board/actions/runs/27032123039`.
- Package publish workflow result: package publish succeeded, then immediate post-publish
  `release:smoke` failed with npm `ETARGET` while the registry was still propagating.
- npm registry state after propagation:

```json
{
  "version": "0.1.1",
  "dist-tags": {
    "latest": "0.1.1"
  },
  "versions": ["0.1.0", "0.1.1"]
}
```

- Local post-propagation release smoke:

```text
npm run release:smoke -- --version 0.1.1
```

Result: passed. The smoke installed `@mean-weasel/bugdrop-board@0.1.1`, resolved the root,
`/board`, and `/board.js` entrypoints to `public/board.js`, confirmed widget/fetch code, and
verified the installed version.

## Follow-Up Already Landed

PR #30 extended the Package Widget workflow post-publish smoke to:

```bash
npm run release:smoke -- --retries 12 --retry-delay-ms 10000
```

This should reduce false-red publish runs caused by npm registry propagation delay.

## Scope Audit

- No hosted control plane.
- No billing.
- No realtime/WebSocket/Durable Objects.
- No comments.
- No downvotes.
- No GitHub Projects.
- No secret rotation or credential edits.
