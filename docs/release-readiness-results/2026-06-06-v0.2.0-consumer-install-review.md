# BugDrop Board v0.2.0 Consumer Install Review

Status: passed.

This post-release review verifies that a fresh consumer can install the published
`@mean-weasel/bugdrop-board@0.2.0` package and boot the documented embedded widget from the package
bundle.

## Registry And Release State

`npm view @mean-weasel/bugdrop-board version dist-tags versions --json` reported:

```json
{
  "version": "0.2.0",
  "dist-tags": {
    "latest": "0.2.0"
  },
  "versions": ["0.1.0", "0.1.1", "0.1.2", "0.2.0"]
}
```

GitHub Release `v0.2.0` is public:

- URL: `https://github.com/mean-weasel/bugdrop-board/releases/tag/v0.2.0`
- Target commit: `582254d80868a6c20bbaa28972660d49487ccbb7`
- Draft: `false`
- Prerelease: `false`

## Clean-Room Install Smoke

Command:

```sh
npm run install:smoke -- --version 0.2.0 --retries 3 --retry-delay-ms 5000
```

Result: passed on install attempt 1.

The smoke installed `@mean-weasel/bugdrop-board@0.2.0` into a temporary project, served only the
installed `public/board.js` bundle, and opened the documented inline embed in Chromium.

Verified package checks:

- Installed version is `0.2.0`.
- Root export, `./board`, and `./board.js` all resolve to `public/board.js`.
- Bundle contains widget code and fetch usage.
- Bundle includes `data-mount-selector` support.
- Bundle size was `18003` bytes.

Verified browser/embed checks:

- Widget mounted into `#feedback-board`.
- Exactly one board root rendered in the host slot and in the body.
- Custom heading rendered: `Clean-room board`.
- Item title rendered: `Clean room install proof`.
- Upvote copy/count rendered.
- GitHub issue link copy rendered.
- Token endpoint was requested.
- Items endpoint was requested.
- Browser console errors were empty.
- Failed network requests were empty.

Observed request sequence:

```text
GET /
GET /vendor/bugdrop-board.js
GET /api/bugdrop-board-token
GET /api/boards/board_mean_weasel_demo/items
```

## Scope Audit

This review did not perform:

- npm publish;
- package version bump;
- Cloudflare deploy;
- credential or secret change;
- hosted control plane work;
- billing;
- realtime transport;
- comments;
- downvotes;
- GitHub Projects;
- product behavior change.
