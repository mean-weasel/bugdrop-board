# T002 Worker Receipt

## Change

Added `docs/hosted-security-and-setup.md`.

## Coverage

The document covers:

- current BugDrop Hosted Beta scope and mode distinction;
- host-signed short-lived bearer tokens;
- board-scoped token claims and stable host user ids;
- max TTL, audience, and issuer checks;
- exact origin allowlist and CORS boundary;
- one upvote per user per idea;
- per-board/per-user throttling defaults;
- GitHub Issue mirroring and no D1 item when GitHub creation fails;
- event payload privacy;
- user-configurable embed and customization settings;
- BugDrop-managed hosted settings;
- current limitations and follow-on control-plane work.

## Verification

Final verification is recorded in `T999-final-audit.md`.
