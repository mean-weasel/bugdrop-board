# Production Dogfood Results - 2026-06-05

## Summary

BugDrop Board production dogfood passed against the real `bugdrop.dev` host app, the production
`board.bugdrop.dev` Worker, Cloudflare D1, and the GitHub production dogfood mirror repo.

## Resources

- Host app URL: `https://bugdrop.dev/board-dogfood`
- Board Worker URL: `https://board.bugdrop.dev`
- Board id: `board_mean_weasel_bugdrop_board_production_dogfood`
- Dogfood repo: `mean-weasel/bugdrop-board-production-dogfood`
- GitHub issue proof: `https://github.com/mean-weasel/bugdrop-board-production-dogfood/issues/1`
- BugDrop host PR: `https://github.com/mean-weasel/bugdrop/pull/197`
- BugDrop route config PR: `https://github.com/mean-weasel/bugdrop/pull/198`
- BugDrop production deploy run: `https://github.com/mean-weasel/bugdrop/actions/runs/27016921758`
- BugDrop Board package version: `@mean-weasel/bugdrop-board@0.1.0`

## Board Worker Smoke

`curl -fsS https://board.bugdrop.dev/health` returned:

```json
{ "status": "ok", "environment": "production", "timestamp": "2026-06-05T13:16:39.547Z" }
```

`curl -fsSI https://board.bugdrop.dev/board.js` returned `HTTP/2 200` with
`content-type: text/javascript`.

## Host App Smoke

`curl -fsS https://bugdrop.dev/board-dogfood` returned `HTTP/2 200` and the embedded script
markers:

```html
src="https://board.bugdrop.dev/board.js"
data-board-id="board_mean_weasel_bugdrop_board_production_dogfood"
data-api-url="https://board.bugdrop.dev" data-token-endpoint="/api/bugdrop-board-token?viewer=a"
```

`curl -fsS https://bugdrop.dev/api/bugdrop-board-token?viewer=b` returned `HTTP/2 200`.
Decoded token claims matched:

```json
{
  "boardId": "board_mean_weasel_bugdrop_board_production_dogfood",
  "externalUserId": "bugdrop-dev-dogfood-b",
  "aud": "bugdrop-board",
  "iss": "bugdrop-board-production-host",
  "tokenShape": "288.43"
}
```

The host-side `BUGDROP_BOARD_TOKEN_SECRET` was set from the preserved production board signing
secret without printing or committing the value.

## Browser/API Dogfood Proof

The production proof created item `Production dogfood item 20260605T125406Z` from Viewer A.

GitHub issue proof:

```json
{
  "createdAt": "2026-06-05T12:54:07Z",
  "number": 1,
  "state": "OPEN",
  "title": "Production dogfood item 20260605T125406Z",
  "url": "https://github.com/mean-weasel/bugdrop-board-production-dogfood/issues/1"
}
```

Viewer B readback through the production Board API using a token minted by `bugdrop.dev` showed:

```json
{
  "title": "Production dogfood item 20260605T125406Z",
  "githubIssueNumber": 1,
  "githubIssueUrl": "https://github.com/mean-weasel/bugdrop-board-production-dogfood/issues/1",
  "upvoteCount": 1,
  "viewerHasUpvoted": true
}
```

The production event stream for the created item contained `item_created` and `upvote_added`
events with cursor `2`.

## Failure Mode Checked

The first host route trigger update made `https://bugdrop.dev/board-dogfood` live but disabled the
public `workers.dev` trigger for the existing BugDrop widget. PR #198 fixed the route config by
making `workers_dev = true` explicit and setting `env.preview.routes = []` so preview deployments
do not try to claim production `bugdrop.dev` routes.

Final live smoke after PR #198 merged confirmed:

- `https://bugdrop.neonwatty.workers.dev/widget.js` returned `HTTP/2 200`, `108784` bytes.
- `https://bugdrop.dev/board-dogfood` returned `HTTP/2 200`.
- `https://bugdrop.dev/api/bugdrop-board-token?viewer=b` returned `HTTP/2 200`.
- The signed token was accepted by `https://board.bugdrop.dev` and read back the production
  dogfood item with one upvote.

## Verification Gates

- `make check`: passed in the BugDrop host worktree for both host route PRs.
- `npx wrangler deploy --dry-run`: passed for the persisted BugDrop route config.
- `npx wrangler deploy --dry-run --env preview`: passed and did not list production routes.
- PR #197 CI and merge-group CI: passed.
- PR #198 CI and merge-group CI: passed after the preview route override.
- BugDrop production deploy run `27016921758`: passed.

## Scope Audit

- No new npm package version was published.
- No hosted control plane was added.
- No billing behavior was added.
- No realtime transport was added.
- No comments, downvotes, or GitHub Projects behavior was added.
- No secret values are recorded in this receipt.

## Rollback

Rollback remains operator-controlled:

1. Revert or disable the BugDrop host routes from PR #197/PR #198.
2. Re-run the BugDrop production deploy workflow from the previous known-good host commit.
3. Re-run **Deploy Worker** for BugDrop Board from the previous known-good production commit if the
   board Worker must be rolled back.
4. Restore previous Worker secrets only if an operator intentionally rotates or replaces them.
5. Re-run `/health`, `/board.js`, `bugdrop.dev/board-dogfood`, and token-to-Board-API smoke checks.
