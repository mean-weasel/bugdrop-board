{
  "name": "bugdrop-board",
  "private": true,
  "description": "Embedded, self-hostable ideas/request board.",
  "type": "module",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=22.12.0 <23",
    "npm": ">=10 <11"
  },
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "build": "tsc",
    "build:widget": "node scripts/build-widget.js",
    "deploy": "node -e \"console.error('Refusing ambiguous deploy. Use npm run deploy:production or npx wrangler deploy --env <name>.'); process.exit(1)\"",
    "deploy:development": "wrangler deploy",
    "deploy:production": "npm run build:widget && wrangler deploy --env production",
    "deploy:check": "npm run build:widget && wrangler deploy --dry-run",
    "deploy:check:development": "npm run deploy:check",
    "deploy:check:production": "npm run build:widget && wrangler deploy --dry-run --env production",
    "deploy:check:preview": "npm run build:widget && npm run validate:preview && wrangler deploy --dry-run --env preview",
    "deploy:smoke": "node scripts/verify-deployed-worker.js",
    "doctor:selfhost": "node scripts/doctor-selfhost.js",
    "deploy:workflow:check": "node scripts/verify-deploy-workflow.js",
    "provision:board": "node scripts/provision-board.js",
    "provision:hosted-board": "node scripts/provision-hosted-board.js",
    "provision:preview": "node scripts/provision-preview.mjs",
    "reset:preview": "node scripts/reset-preview-board.mjs",
    "validate:preview": "node scripts/validate-preview-config.mjs",
    "dogfood:seed-kanban": "node scripts/seed-dogfood-kanban-statuses.js",
    "staging:secrets": "node scripts/staging-secrets.js",
    "test": "vitest run",
    "test:node": "vitest run --config vitest.node.config.ts",
    "test:e2e": "playwright test",
    "test:e2e:preview": "playwright test --config playwright.preview.config.ts",
    "test:preview-canary-fixture": "vitest run test/github-issue-canary.test.ts",
    "test:preview-fork-gates": "node scripts/preview-workflow-contract.mjs && vitest run test/preview-workflow-contract.test.ts",
    "capture:marketing-examples": "BUGDROP_BOARD_MARKETING_SCREENSHOT_DIR=docs/marketing/assets playwright test e2e/customization-widget.spec.ts",
    "e2e:worker": "npm run build:widget && CI=1 wrangler d1 migrations apply DB --local && wrangler dev --ip 127.0.0.1 --port 8788 --var ENVIRONMENT:e2e --var BOARD_TOKEN_SECRET:e2e-secret --var BOARD_TOKEN_AUDIENCE:bugdrop-board --var BOARD_TOKEN_ISSUER:dummy-host --var ALLOWED_ORIGINS:http://127.0.0.1:5177 --log-level error",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit && tsc --project tsconfig.widget.json --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "knip": "knip",
    "audit": "npm audit --audit-level=critical",
    "check:actions-node24": "node -e \"const {execSync}=require('node:child_process'); const out=execSync('rg -n \\\\\\\"actions/(checkout|setup-node|cache|upload-artifact)@v4|cloudflare/wrangler-action\\\\\\\" .github/workflows || true',{encoding:'utf8'}); if(out.trim()){ console.error(out); process.exit(1); } console.log('GitHub Actions are Node 24-ready');\"",
    "validate": "npm run lint && npm run format:check && npm run typecheck && npm run deploy:workflow:check && npm run test",
    "release:rehearsal": "npm run provision:board -- --repo mean-weasel/release-rehearsal --name \"Release Rehearsal\" --local && npm run deploy:check && npm run test:e2e && npm run validate && npm run knip && npm run audit && npm run check:actions-node24",
    "prepublishOnly": "node -e \"console.error('BugDrop Board is deployed as a Worker and is not published to npm.'); process.exit(1)\"",
    "prepare": "husky"
  },
  "dependencies": {
    "hono": "^4.13.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.3",
    "@cloudflare/workers-types": "^5.20260814.1",
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@eslint/js": "^9.39.2",
    "@playwright/test": "^1.60.0",
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^4.1.6",
    "esbuild": "^0.28.2",
    "eslint": "^9.39.2",
    "globals": "^16.5.0",
    "husky": "^9.1.7",
    "jsdom": "^29.0.1",
    "knip": "^5.76.1",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.50.0",
    "vitest": "^4.1.6",
    "wrangler": "^4.123.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{js,jsx,json,md,css,yml,yaml}": [
      "prettier --write"
    ]
  }
}
