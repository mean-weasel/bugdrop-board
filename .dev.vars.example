# Local-only secrets. Copy this file to .dev.vars and fill in real values.
# Do not commit .dev.vars or use it for deployed Workers.
# Deployed Workers should receive these with `wrangler secret put`.

BOARD_TOKEN_SECRET=replace-with-a-long-random-secret
GITHUB_ISSUE_ACCESS_TOKEN=github-token-that-can-create-issues
